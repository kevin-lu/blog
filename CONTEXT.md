# CONTEXT.md - 领域术语表

本文档定义本项目的领域术语和核心概念，确保开发团队和 AI 使用一致的语言。

## 核心概念

### 文章 (Article)
博客的核心内容单元，包含标题、内容、封面图等元数据。文章可以是草稿、已发布或归档状态。

### 分类 (Category)
文章的层级分类，用于组织内容。一篇文章可以属于多个分类。

### 标签 (Tag)
文章的扁平化标签，用于标记内容特征。一篇文章可以有多个标签。

### 评论 (Comment)
用户对文章的反馈，支持嵌套回复。评论需要审核后才能显示。

### 点赞 (Like)
用户对文章的喜爱表达，无需登录即可操作。同一用户/会话对同一篇文章只能点赞一次。

### 会话 (Session)
未登录用户的临时标识，基于浏览器 session_id 和 IP 地址组合。

### 浏览记录 (Visit)
用户访问文章的行为记录，包含 IP 地址、访问时间、User-Agent 等信息。用于数据分析和安全防护。

### PV (Page View)
页面浏览量，每次访问都计数。用于衡量文章的热度。

### UV (Unique Visitor)
独立访客数，同一天内同一 IP 的多次访问只计为 1 个 UV。用于衡量文章的覆盖范围。

## 技术术语

### 点赞计数策略
- **实时计算**：点赞数通过 `COUNT(article_likes)` 实时查询，适用于数据量小的场景
- **缓存计数**：点赞数存储在 Redis 和文章表字段，适用于高并发场景

### 限流策略
- **IP 限流**：同一 IP 每分钟最多 N 次操作，防止刷赞攻击

### 浏览计数策略
- **PV 计数**：每次访问都增加浏览次数（防刷：同一 IP 24 小时内只计一次）
- **UV 统计**：按天统计独立访客数（同一天内同一 IP 只计 1 个 UV）
- **IP 存储**：明文存储 IP 地址，用于数据分析和安全防护

## 项目结构

- **Backend**: Python/Flask REST API
- **Frontend**: Vue 3 + Vite + TypeScript
- **Database**: MySQL + SQLAlchemy ORM
- **Cache**: Redis (可选)

## 命名约定

- 数据库表名：复数形式，蛇形命名（如 `article_likes`）
- 模型类名：单数形式，大驼峰命名（如 `ArticleLike`）
- API 路由：复数形式，短横线命名（如 `/articles/<id>/like`）
